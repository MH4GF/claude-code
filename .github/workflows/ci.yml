name: CI

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unslop:
    uses: MH4GF/shared-config/.github/workflows/_unslop.yml@7214dc3afc9d49e8f0341f8f0deae6767be03600 # 7214dc3
    with:
      config: .textlintrc.json
