name: CI

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unslop:
    uses: MH4GF/shared-config/.github/workflows/_unslop.yml@5378bfce550384e1e9a83504ab2a71df28cc6920 # 5378bfc
    with:
      config: .textlintrc.json
